# postgres-env-docker

Ambiente PostgreSQL com o uso do Docker

# Iniciar o ambiente

```bash
docker-compose up -d
```

# Derrubar o ambiente

```bash
docker-compose down
```

# Derrubar o ambiente e zerar o banco

```bash
docker-compose down -v
```
